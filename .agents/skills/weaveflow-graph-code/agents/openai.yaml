interface:
  display_name: "WeaveFlow Graph Code"
  short_description: "Implement Graph code with API handoff"
  default_prompt: "Use $weaveflow-graph-code to implement and validate a focused WeaveFlow Graph runtime change, then separate any authorized API Session and Run quality checks."
