interface:
  display_name: "WeaveFlow Graph Debug"
  short_description: "Audit and debug Runs via public API"
  default_prompt: "Use $weaveflow-graph-debug to reconstruct a Run's exact Session context, produce a four-part persisted quality audit, diagnose it, and recover it safely only when authorized."
