interface:
  display_name: "WeaveFlow Graph"
  short_description: "Upload, run, and debug WeaveFlow agent graphs"
  default_prompt: "Use $weaveflow-graph to configure, upload, run, and debug one complete graph session through the WeaveFlow server API."
