interface:
  display_name: "WeaveFlow Graph Create"
  short_description: "Author Graphs with API preflight"
  default_prompt: "Use $weaveflow-graph-create to understand the API-exposed Graph context, preflight model/tool permissions and approvals, validate its definition, and create an immutable Session only when authorized."
